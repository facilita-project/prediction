'use strict'

module.exports = {
  helmet: require('./middlewares/helmet'),
  morgan: require('./middlewares/morgan'),
  deeptrace: require('./middlewares/deep-trace'),
  normalizer: require('./middlewares/normalizer'),
  parsers: {
    json: require('./middlewares/parsers-json'),
    urlencoded: require('./middlewares/parsers-urlencoded')
  },
  renderer: require('./middlewares/renderer'),
  sentry: {
    errors: require('./middlewares/sentry-errors'),
    requests: require('./middlewares/sentry-requests')
  },
  stderr: require('./middlewares/stderr'),
  routes: {
    unmatched: require('./middlewares/routes-unmatched')
  },
  validation: {
    error: require('./middlewares/validation-error'),
    schema: require('./middlewares/validation-schema')
  }
}
